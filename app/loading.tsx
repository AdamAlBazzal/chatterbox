import React from "react";

const loading = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <div className="p-4 bg-primary rounded-lg">loading...</div>
    </div>
  );
};

export default loading;
